---
"@repobuddy/upx": patch
---

Correct the `npx` per-call latency in the README. The prose claimed ~1s, contradicting the measurement table on the same page, which records a 315ms median on npm 12.
